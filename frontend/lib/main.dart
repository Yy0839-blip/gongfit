import 'package:flutter/material.dart';
import 'core/theme/app_theme.dart';
import 'data/models/profile.dart';
import 'data/models/profile_store.dart';
import 'data/repositories/gongfit_repository.dart';
import 'presentation/viewmodels/analyze_view_model.dart';
import 'presentation/viewmodels/chat_view_model.dart';

Future<void> main() async { WidgetsFlutterBinding.ensureInitialized(); await profileStore.load(); runApp(const GongfitApp()); }
class GongfitApp extends StatelessWidget{const GongfitApp({super.key});@override Widget build(BuildContext c)=>MaterialApp(debugShowCheckedModeBanner:false,title:'공핏',theme:AppTheme.light(),home:const HomePage());}
class HomePage extends StatelessWidget{const HomePage({super.key});@override Widget build(BuildContext c)=>Scaffold(body:SafeArea(child:ListView(padding:const EdgeInsets.fromLTRB(22,18,22,32),children:[Row(children:[Container(width:44,height:44,decoration:BoxDecoration(color:Theme.of(c).colorScheme.primary,borderRadius:BorderRadius.circular(14)),child:const Icon(Icons.auto_awesome,color:Colors.white)),const SizedBox(width:12),const Text('공핏',style:TextStyle(fontSize:24,fontWeight:FontWeight.w800)),const Spacer(),IconButton(onPressed:(){},icon:const Icon(Icons.notifications_none_rounded))]),const SizedBox(height:34),const Text('내게 맞는 공공기관\n취업을 시작해보세요.',style:TextStyle(fontSize:31,fontWeight:FontWeight.w800,height:1.18)),const SizedBox(height:10),const Text('채용공고를 분석하고 나와 얼마나 잘 맞는지 확인하세요.',style:TextStyle(color:Colors.black54,fontSize:15)),const SizedBox(height:26),_Hero(onTap:()=>Navigator.push(c,MaterialPageRoute(builder:(_)=>const AnalyzePage()))),const SizedBox(height:18),Row(children:[Expanded(child:_Feature(icon:Icons.person_outline,title:'내 프로필',sub:profileStore.profile.targetJob.isEmpty?'스펙 관리':profileStore.profile.targetJob,tap:()=>Navigator.push(c,MaterialPageRoute(builder:(_)=>const ProfilePage())))),const SizedBox(width:12),Expanded(child:_Feature(icon:Icons.chat_bubble_outline,title:'공핏 AI',sub:'취업 상담',tap:()=>Navigator.push(c,MaterialPageRoute(builder:(_)=>const ChatPage()))))]),const SizedBox(height:28),const Text('공핏이 해드리는 것',style:TextStyle(fontSize:19,fontWeight:FontWeight.w800)),const SizedBox(height:12),const _Info(icon:Icons.analytics_outlined,title:'AI 직무 적합도',text:'지원 가능 여부와 직무 적합도를 한눈에'),const _Info(icon:Icons.track_changes_outlined,title:'맞춤 준비 전략',text:'부족한 역량과 지금 해야 할 일을 제안'),const _Info(icon:Icons.psychology_outlined,title:'AI 취업 상담',text:'내 공고와 프로필을 이해하는 AI 상담')])));}}
class _Hero extends StatelessWidget{final VoidCallback onTap;const _Hero({required this.onTap});@override Widget build(BuildContext c)=>InkWell(onTap:onTap,borderRadius:BorderRadius.circular(28),child:Container(padding:const EdgeInsets.all(24),decoration:BoxDecoration(color:Theme.of(c).colorScheme.primary,borderRadius:BorderRadius.circular(28)),child:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Icon(Icons.description_rounded,color:Colors.white,size:34),SizedBox(height:22),Text('채용공고 분석하기',style:TextStyle(color:Colors.white,fontSize:21,fontWeight:FontWeight.w800)),SizedBox(height:7),Text('공고를 붙여넣으면 공핏이\n지원 전략을 만들어드려요.',style:TextStyle(color:Colors.white70,fontSize:14,height:1.5)),SizedBox(height:20),Row(children:[Text('지금 시작하기',style:TextStyle(color:Colors.white,fontWeight:FontWeight.w700)),SizedBox(width:6),Icon(Icons.arrow_forward_rounded,color:Colors.white,size:18)])])));}
class _Feature extends StatelessWidget{final IconData icon;final String title,sub;final VoidCallback tap;const _Feature({required this.icon,required this.title,required this.sub,required this.tap});@override Widget build(BuildContext c)=>InkWell(onTap:tap,borderRadius:BorderRadius.circular(22),child:Container(padding:const EdgeInsets.all(18),decoration:BoxDecoration(color:Colors.white,borderRadius:BorderRadius.circular(22),boxShadow:[BoxShadow(color:Colors.black.withOpacity(.04),blurRadius:20,offset:const Offset(0,8))]),child:Row(children:[Icon(icon,size:27),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(title,style:const TextStyle(fontWeight:FontWeight.w800)),const SizedBox(height:3),Text(sub,overflow:TextOverflow.ellipsis,style:const TextStyle(color:Colors.black45,fontSize:12))]))])));}
class _Info extends StatelessWidget{final IconData icon;final String title,text;const _Info({required this.icon,required this.title,required this.text});@override Widget build(BuildContext c)=>Padding(padding:const EdgeInsets.only(bottom:10),child:Container(padding:const EdgeInsets.all(16),decoration:BoxDecoration(color:Colors.white,borderRadius:BorderRadius.circular(18)),child:Row(children:[Icon(icon),const SizedBox(width:14),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(title,style:const TextStyle(fontWeight:FontWeight.w700)),const SizedBox(height:4),Text(text,style:const TextStyle(color:Colors.black54,fontSize:13))]))])));}
class AnalyzePage extends StatefulWidget{const AnalyzePage({super.key});@override State<AnalyzePage> createState()=>_AnalyzeState();}
class _AnalyzeState extends State<AnalyzePage>{final text=TextEditingController();final vm=AnalyzeViewModel(GongfitRepository());@override void dispose(){text.dispose();vm.dispose();super.dispose();}@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('채용공고 분석',style:TextStyle(fontWeight:FontWeight.w800))),body:AnimatedBuilder(animation:vm,builder:(_,__)=>ListView(padding:const EdgeInsets.all(20),children:[const Text('채용공고를 붙여넣으세요',style:TextStyle(fontSize:25,fontWeight:FontWeight.w800)),const SizedBox(height:8),Text('현재 프로필: ${profileStore.profile.targetJob.isEmpty?'미설정':profileStore.profile.targetJob}',style:const TextStyle(color:Colors.black54)),const SizedBox(height:22),TextField(controller:text,maxLines:14,onChanged:(_)=>setState((){}),decoration:const InputDecoration(hintText:'채용공고 전체 내용을 붙여넣어 주세요.')),const SizedBox(height:16),if(vm.error!=null)Text(vm.error!,style:TextStyle(color:Theme.of(c).colorScheme.error)),SizedBox(height:52,width:double.infinity,child:FilledButton(onPressed:vm.loading||text.text.trim().isEmpty?null:()=>vm.analyze(text.text,profileStore.profile),child:vm.loading?const SizedBox(width:22,height:22,child:CircularProgressIndicator(strokeWidth:2)):const Text('공핏 분석 시작',style:TextStyle(fontWeight:FontWeight.w700)))),if(vm.result!=null)...[const SizedBox(height:24),_ResultCard(result:vm.result!)]])));}}
class _ResultCard extends StatelessWidget{final Map<String,dynamic> result;const _ResultCard({required this.result});@override Widget build(BuildContext c){final a=(result['analysis'] as Map?)?.cast<String,dynamic>()??{};final score=a['score']??a['match_score']??0;final strengths=(a['strengths'] as List?)?.cast<dynamic>()??[];final gaps=(a['gaps'] as List?)?.cast<dynamic>()??[];final recommendation=a['recommendation']??a['summary']??'분석 결과를 확인해 보세요.';return Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('분석 결과',style:TextStyle(fontSize:22,fontWeight:FontWeight.w800)),const SizedBox(height:14),Container(width:double.infinity,padding:const EdgeInsets.all(22),decoration:BoxDecoration(color:Colors.white,borderRadius:BorderRadius.circular(24)),child:Column(children:[Text('$score',style:TextStyle(fontSize:54,fontWeight:FontWeight.w900,color:Theme.of(c).colorScheme.primary)),const Text('공핏 적합도',style:TextStyle(color:Colors.black54)),const SizedBox(height:18),LinearProgressIndicator(value:(score is num?(score/100).clamp(0,1):0).toDouble(),minHeight:9,borderRadius:BorderRadius.circular(9)),const SizedBox(height:18),Text('$recommendation',textAlign:TextAlign.center,style:const TextStyle(fontWeight:FontWeight.w700,height:1.45))])),const SizedBox(height:14),_ListCard(title:'나의 강점',icon:Icons.thumb_up_alt_outlined,items:strengths),const SizedBox(height:12),_ListCard(title:'보완할 점',icon:Icons.flag_outlined,items:gaps),const SizedBox(height:12),SizedBox(width:double.infinity,height:50,child:OutlinedButton.icon(onPressed:()=>Navigator.push(c,MaterialPageRoute(builder:(_)=>const ChatPage())),icon:const Icon(Icons.auto_awesome),label:const Text('이 결과를 공핏 AI에게 물어보기'))) ]);}}
class _ListCard extends StatelessWidget{final String title;final IconData icon;final List<dynamic> items;const _ListCard({required this.title,required this.icon,required this.items});@override Widget build(BuildContext c)=>Container(width:double.infinity,padding:const EdgeInsets.all(18),decoration:BoxDecoration(color:Colors.white,borderRadius:BorderRadius.circular(20)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[Icon(icon,size:20),const SizedBox(width:8),Text(title,style:const TextStyle(fontWeight:FontWeight.w800))]),const SizedBox(height:12),if(items.isEmpty)const Text('분석 결과가 없습니다.',style:TextStyle(color:Colors.black45))else...items.map((e)=>Padding(padding:const EdgeInsets.only(bottom:7),child:Row(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('•  '),Expanded(child:Text('$e'))]))) ]));}
class ProfilePage extends StatefulWidget{const ProfilePage({super.key});@override State<ProfilePage> createState()=>_ProfileState();}
class _ProfileState extends State<ProfilePage>{late final TextEditingController job,major,certs,experience;@override void initState(){super.initState();final p=profileStore.profile;job=TextEditingController(text:p.targetJob);major=TextEditingController(text:p.major);certs=TextEditingController(text:p.certifications);experience=TextEditingController(text:p.experience);}@override void dispose(){job.dispose();major.dispose();certs.dispose();experience.dispose();super.dispose();}@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('내 프로필',style:TextStyle(fontWeight:FontWeight.w800))),body:ListView(padding:const EdgeInsets.all(20),children:[const Text('나를 알려주세요',style:TextStyle(fontSize:25,fontWeight:FontWeight.w800)),const SizedBox(height:8),const Text('공핏 분석에 내 정보가 실제로 반영됩니다.',style:TextStyle(color:Colors.black54)),const SizedBox(height:24),TextField(controller:job,decoration:const InputDecoration(labelText:'희망 직무',hintText:'예: 공공기관 행정')),const SizedBox(height:12),TextField(controller:major,decoration:const InputDecoration(labelText:'전공')),const SizedBox(height:12),TextField(controller:certs,decoration:const InputDecoration(labelText:'자격증',hintText:'쉼표로 구분')),const SizedBox(height:12),TextField(controller:experience,maxLines:5,decoration:const InputDecoration(labelText:'경력 · 프로젝트')),const SizedBox(height:8),SizedBox(height:52,child:FilledButton(onPressed:()async{await profileStore.update(targetJob:job.text,major:major.text,certifications:certs.text,experience:experience.text);if(!mounted)return;ScaffoldMessenger.of(c).showSnackBar(const SnackBar(content:Text('프로필을 저장했습니다.')));setState((){});},child:const Text('프로필 저장',style:TextStyle(fontWeight:FontWeight.w700))))]));}}
class ChatPage extends StatefulWidget{const ChatPage({super.key});@override State<ChatPage> createState()=>_ChatState();}
class _ChatState extends State<ChatPage>{final input=TextEditingController();final vm=ChatViewModel(GongfitRepository());@override void dispose(){input.dispose();vm.dispose();super.dispose();}@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('공핏 AI',style:TextStyle(fontWeight:FontWeight.w800))),body:Column(children:[Expanded(child:AnimatedBuilder(animation:vm,builder:(_,__)=>ListView(padding:const EdgeInsets.all(18),children:[if(vm.messages.isEmpty)const Padding(padding:EdgeInsets.only(top:100),child:Column(children:[Icon(Icons.auto_awesome,size:42),SizedBox(height:16),Text('무엇이든 물어보세요',style:TextStyle(fontSize:22,fontWeight:FontWeight.w800)),SizedBox(height:8),Text('공고, 자소서, 면접, 취업전략까지\n공핏 AI가 함께 준비합니다.',textAlign:TextAlign.center,style:TextStyle(color:Colors.black54))]),...vm.messages.map((m)=>Align(alignment:m.user?Alignment.centerRight:Alignment.centerLeft,child:Container(margin:const EdgeInsets.only(bottom:10),padding:const EdgeInsets.all(14),constraints:const BoxConstraints(maxWidth:340),decoration:BoxDecoration(color:m.user?const Color(0xFF155EEF):Colors.white,borderRadius:BorderRadius.circular(18)),child:Text(m.text,style:TextStyle(color:m.user?Colors.white:Colors.black87,height:1.45))))]))),SafeArea(child:Padding(padding:const EdgeInsets.fromLTRB(14,8,14,14),child:Row(children:[Expanded(child:TextField(controller:input,minLines:1,maxLines:4,decoration:const InputDecoration(hintText:'공핏 AI에게 물어보세요'))),const SizedBox(width:8),IconButton.filled(onPressed:vm.loading?null:(){final t=input.text.trim();if(t.isEmpty)return;input.clear();vm.send(t,profile:profileStore.profile);},icon:const Icon(Icons.arrow_upward_rounded))]))]));}}
